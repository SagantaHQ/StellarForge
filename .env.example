# Soroban.Build — Environment Variables
# Copy this file to .env and fill in your values.

# Database (NON-NEGOTIABLE: always PostgreSQL, never SQLite)
DATABASE_URL=
DIRECT_DATABASE_URL=

# GitHub token for server-side API calls (commits, PRs, status checks)
GITHUB_SERVER_TOKEN=
